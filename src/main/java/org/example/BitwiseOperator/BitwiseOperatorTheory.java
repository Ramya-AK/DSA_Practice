package org.example.BitwiseOperator;

public class BitwiseOperatorTheory {


    // Xor = > any one should be true, then only true
    // 0 xor 0 => 0
    // 0 xor 1 => 1
    // 1 xor 0 => 1
    // 1 xor 1 => 1


    // binary 0 and 1 = > base 2
    // octa = > 0 to 7 => base 8 => 0 to 7, 10, 11,12...
    // hexa = > 0 to 9, a to  f

//    left shift
//    a << 1 = 2a
//    a<< b = a * 2(power of b)


    // right shift
    //  a>>b
    //  a>>b = a/ (2(power of b))


    // decimal to binary
    // 20 / 2 => 0
    // 10 / 2 => 0
    // 5  / 2 => 0
    // 2 / 2 => 1
    // 1 / 2 => 0

    // answer => 101000


//    binary to decimal
//    nnumer * 2(power of 4) + nnumer * 2(power of 3) + nnumer * 2(power of 2) + nnumer * 2(power of 1) + nnumer * 2(power of 1)
}
