package org.example.BinarySearch;

public class BinarySearchTheory {

    // Binary search apply when array is sorted
    // Binary search apply when repeatedly call => to get the answer if you are doing the sequency to get that
    // example => sqaure root of number
    // IMP => always find the solution in the left right direction but not both ways
}
